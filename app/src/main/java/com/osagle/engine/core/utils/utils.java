package com.osagle.engine.core.utils;

public class utils {
    public static double clamp(double a, double b, double c) {
        return Math.max(b,Math.min(c,a));
    }
    public static int getKey(String[] pArray, String pValue) {
        return 0;
    }
}
